---
title: 'NoSQL 注入 (6个实例)'
severity: "紧急"
severity_order: 5
cvss_score: "9.4"
date_discovered: "2026-03-17 10:11:30"
type: 'NoSQL 注入'
status: "待修复"
cve: null
cwe:
  - "89"
tags:
  - 'NoSQL 注入'
system:
  - '区块链+刑罚执行监督系统'
vul_category: '应用系统漏洞'
created: "2026-06-11 17:12"
---

## 原因
未对用户输入正确执行危险字符清理
未对用户输入正确执行危险字符清理

## 修复建议
确保用户输入的类型正确并对其进行正确转义
常规
1. 要缓解该问题，将接收到的参数转换为正确的类型（例如字符串）。
2. 在可能的情况下对接收查询的函数使用对象而不是字符串。否则，对用户输入进行正确地转义。

### 风险
可以查看、变更或删除数据库条目和表格
该软件使用受外部影响的输入构建全部或部分 NoSQL DB 查询，但无法中和在将查询发送到数据库时可能修改查询的元素。
如果没有足够的类型检查或对用户可控制的输入进行转义，则生成的查询可能会导致这些输入被解释为查询的一部分而不是普通的用户数据。
这两个变量（$user 和 $pass）包含了用户在登录表单中输入的用户凭证。如果用户输入“jsmith”作为用户名，并输入“Demo1234”作为密码，那么查询将如下所示：
例如，假设我们有一个带有登录表单的 HTML 页面，该页面最终使用以下 PHP 代码中的用户输入在数据库上运行以下 MongoDB 查询：
db->logins->find(array("用户名"=>$user, "密码"=>$pass));
$user 和 $pass 这两个变量包含用户在登录表单中输入的用户凭证。
如果用户输入“jsmith”作为用户名，输入“Demo1234”作为密码，则查询将如下所示：
db.logins.find（{ username: "jsmith", password: "Demo1234" } ）
但是如果用户发送“user[$ne]=1”而不是用户参数，发送“pass[$ne]=1”而不是密码参数，则查询将如下所示（PHP 将用户和密码参数视为关联数组，其中一个元素“$ne”的值为 1）：
db.logins.find（{ username: { $ne: 1 } ， 密码：{ $ne: 1 } }）
由于$ne是NoSQL中的“不等于”条件，此查询将查找登录集合中用户名不等于1且密码不等于1的所有条目，这意味着此查询肯定会返回登录集合中的所有用户。在这种情况下，该漏
洞将导致攻击者无需有效的用户名和密码即可登录应用程序。

### 受影响产品
此问题可能影响不同类型的产品。
## 扫描原始数据
- **原始报告**：[[raw/pdf/3-17.pdf#page=9|查看报告 (第9页)]]

**原始报告截图（第9页）：**
![[../assets/screenshots/VL-2026-026_p9.png|1000]]
- *此为代表性样本，同类实例仅URL/参数不同*
## 沟通记录
### 建设方拒绝修复
如建设方拒绝修复，记录：
- 拒绝日期
- 拒绝理由
- 建设方提供的"替代措施"
- 风险评估

### 检测方回复策略
我方针对拒绝的回复策略

## 状态追踪
- [x] 2026-03-17 10:11:30 初次发现
- [ ] 建设方确认接收
- [ ] 建设方提供修复计划
- [ ] 修复完成
- [ ] 验证通过

## 相关链接
- [[区块链+刑罚执行监督系统]] - 受影响系统
- [[NoSQL 注入]] - 漏洞类型详解
## 受影响实例


| # | URL |
|---|---|
| 1 | `https://10.249.2.1:8090/admin-api/system/captcha/check` |
| 2 | `https://10.249.2.1:8090/admin-api/system/auth/login` |
| 3 | `https://10.249.2.1:8090/admin-api/system/captcha/get` |
| 4 | `https://10.249.2.1:8090/admin-api/chain/jfkh-mc/page` |
| 5 | `https://10.249.2.1:8090/admin-api/chain/jfkh-jg/listByYjmcIdOrYjId` |
| 6 | `https://10.249.2.1:8090/admin-api/chain/shouye/gzList` |

