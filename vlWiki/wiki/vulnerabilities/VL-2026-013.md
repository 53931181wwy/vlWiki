---
title: '具有不安全、不正确或缺少 SameSite 属性的 Cookie (1个实例)'
severity: "中"
severity_order: 3
cvss_score: "4.7"
date_discovered: "2026-03-03"
created: "2026-05-26 18:11"
type: '具有不安全、不正确或缺少 SameSite 属性的 Cookie'
status: "待修复"
cve: null
cwe:
  - "1275"
  - "284"
  - "923"
tags:
  - '具有不安全、不正确或缺少 SameSite 属性的 Cookie'
system:
  - '数据加密服务系统-26-3-3'
vul_category: '应用系统漏洞'
---

# 具有不安全、不正确或缺少 SameSite 属性的 Cookie

## 原因
具有不正确、不安全或缺少 SameSite 属性的敏感 Cookie
具有不正确、不安全或缺少 SameSite 属性的敏感 Cookie

## 修复建议
查看将 SameSite Cookie 属性配置为推荐值的可能解决方案
常规
[1] 查看将 SameSite Cookie 属性配置为推荐值的可能解决方案。[2] 将 Cookie 限制为第一方或同一站点上下文。[3] 验证并将 Cookie 的 SameSite 属性设置为 Strict，以确保仅在第一
方上下文中发送 Cookie。[4] 或者，如果要放宽第一方上下文的限制，请验证 Cookie 的 SameSite 属性并将其设置为 Lax，同时启用 Secure 标志并通过 HTTPS 进行传输。

### 风险
通过将 Cookie 限制为第一方或同一站点上下文，防止 Cookie 信息泄露
如果没有额外的保护措施（例如反 CSRF 令牌），攻击可能会扩展到跨站点请求伪造 (CSRF) 攻击。
SameSite 属性控制如何为跨域请求发送 Cookie。
该属性可以有三个值：“Lax”、“Strict”或“None”。如果使用“None”，Web 站点可能会向另一个 Web 站点创建跨域 POST HTTP 请求，浏览器会自动向该请求添加 Cookie。
如果没有适当的保护措施（例如反 CSRF 令牌），则可能会导致跨站请求伪造 (CSRF) 攻击。
模式及其用途：
“Lax”模式：Cookie 将仅随顶级 GET 请求一起发送。“Strict”模式；即使用户点击另一个 Web 站点的链接，也不会以任何跨站点使用方式发送该 Cookie。“None”模式：Cookie 将随
跨站点请求一起发送。
具有“Lax”或“None”的属性必须设置“Secure”标志，并且必须通过 https 传输。示例 - Set-Cookie: key=value; SameSite=Lax;Secure
建议将属性设置为“Strict”。
示例 - Set-Cookie: key=value; SameSite=Strict

### 受影响产品
此问题可能影响不同类型的产品。

### 外部引用
WASC 威胁分类：信息泄露
SameSite Cookie
## 受影响实例


| # | URL |
|---|---|
| 1 | `https://10.132.197.11:9044/` |

## 扫描原始数据
- **原始报告**：[[raw/pdf/26-3-3 安全报告.pdf#page=8|查看报告 (第8页)]]

**原始报告截图（第8页）：**
![[../assets/screenshots/VL-2026-013_p8.png|1200]]
- *此为代表性样本，同类实例仅URL/参数不同*
## 沟通记录
### 建设方拒绝修复
如建设方拒绝修复，记录：
- 拒绝日期
- 拒绝理由
- 建设方提供的"替代措施"
- 风险评估

### 检测方回复策略
我方针对拒绝的回复策略

## 状态追踪
- [x] 2026-03-03 初次发现
- [ ] 建设方确认接收
- [ ] 建设方提供修复计划
- [ ] 修复完成
- [ ] 验证通过

## 相关链接
- [[数据加密服务系统-26-3-3]] - 受影响系统
- [[具有不安全、不正确或缺少 SameSite 属性的 Cookie]] - 漏洞类型详解
